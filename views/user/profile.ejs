<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= userVer.name %> - Profile</title>
    <link rel="icon" href="/images/logo.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+3:ital,wght@0,200..900;1,200..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <style>
        /* Navbar */
        .navbar {
            width: 100%;
            padding: 15px 0;
            box-shadow: 10px 10px 15px rgba(0, 0, 0, 0.5);
        }

        .navbar-brand {
            font-size: 1.5rem;
            font-weight: 700;
            color: #8CBF3A;
        }

        .navbar-nav .nav-link {
            font-weight: 500;
            color: #555;
            font-size: 1rem;
            margin: 0 10px;
        }

        .navbar-nav .nav-link:hover {
            color: #8CBF3A;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        img {
            max-width: 100%;
            height: auto;
        }

        body {
            font-family: 'Source Sans 3', sans-serif;
        }

        .sidebar {
            background-color: #f8f9fa;
            padding: 20px;
            height: 100%;
        }

        .inputs-data{
            display: flex;
            flex-direction: row;
        }

        .profile-section {
            padding: 30px;
        }

        .card{
            background-color: #55555543;
            align-items: center;
        }

        .profile-section input {
            width: 100%;
            margin-bottom: 15px;
        }

        /* Styling the profile box */
        .profile-box {
            width: 100%;
            max-width: 600px; /* Set max width for the table */
            margin: 20px auto; /* Center the box */
            padding: 10px;
            background-color: #f9f9f9; 
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 10px 10px 15px rgba(0, 0, 0, 0.5);
        }

        /* Styling the table */
        table {
            width: 100%; /* Full width */
            border-collapse: collapse; /* Removes gaps between table cells */
        }

        td {
            padding: 10px; /* Add padding inside cells */
            vertical-align: middle; /* Align content vertically */
        }

        /* Styling labels */
        .label-data label {
            font-weight: bold;
            font-size: 14px;
            color: #333;
            display: block; /* Ensures proper alignment */
        }

        /* Styling inputs */
        input {
            width: 100%; /* Inputs take full width of the cell */
            padding: 8px;
            font-size: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
            background-color: #f5f5f5;
        }

        .btn-primary{
            width: auto;
            margin: 10px 0;
            background-color: #8cbf3a;
            border: 1px solid #c8c4c4;
        }

        .btn-primary:hover{
            background-color: #67892f;
            border: 1px solid #797979;
        }

        /* Adding responsiveness */
        @media (max-width: 600px) {
            table {
                display: block; /* Make table scrollable */
                overflow-x: auto;
            }

            td {
                display: block; /* Stack cells vertically */
                width: 100%;
            }
        }



        .footer {
            background-color: #8dc63f;
            color: white;
            padding: 20px;
        }

        .footer a {
            color: white;
            text-decoration: none;
        }

        .toast-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
        }

        .toast {
            display: none;
            background-color: #ff6600;
            color: white;
            padding: 15px 30px;
            border-radius: 12px;
            font-size: 16px;
            text-align: center;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            opacity: 0;
            transform: translateX(100%);
            animation: slideInFromRight 0.8s ease-out forwards;
        }

        @keyframes slideInFromRight {
            0% {
                opacity: 0;
                transform: translateX(100%);
            }

            100% {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideOutToRight {
            0% {
                opacity: 1;
                transform: translateX(0);
            }

            100% {
                opacity: 0;
                transform: translateX(100%);
            }
        }

    </style>
</head>

<body>
    <!-- Header Section -->
    <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container">
                <a class="navbar-brand" href="#">
                    <img src="/images/logo.png" style="width: 30px;">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                        <li class="nav-item"><a class="nav-link" href="/shop">Shop</a></li>
                        <li class="nav-item"><a class="nav-link" href="/cart">Cart</a></li>
                        <li class="nav-item"><a class="nav-link" href="/wishlist">Wishlist</a></li>
                    </ul>
                    <!-- User Profile Section -->
                    <div class="dropdown ms-3">
                        <a href="#" class="d-flex align-items-center text-decoration-none "
                            id="userProfileDropdown"  aria-expanded="false">
                            <img src="/images/profile.png" style="width: 30px; height: 30px; border-radius: 50%;">
                            <span class="ms-2">
                                <%= userVer.name %>
                            </span>
                        </a>
                    </div>
                    <!-- End of User Profile Section -->
                </div>
            </div>
        </nav>
    </header>
    <div class="toast-container"></div>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 sidebar">
                <nav class="mt-4">
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link" href="">Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/orders">Orders</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="">Wallet</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/address">Addresses</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="" onclick="document.getElementById('logoutForm').submit(); return false;">Logout</a>
                        </li>
                    </ul>
                </nav>
            </div>

            <!-- Main Content -->
            <div class="col-md-10">
                <!-- Profile Section -->
                <div class="profile-section">
                    <h3>Profile</h3>
                    <div class="card p-4">
                        <div class="profile-box">
                            <table>
                                <tr>
                                    <td class="label-data"><label>Full Name</label></td>
                                    <td><input type="text" value="<%= userVer.name %>" disabled></td>
                                </tr>
                                <tr>
                                    <td class="label-data"><label>Phone Number</label></td>
                                    <td><input type="text" value="<%= userVer.phone %>" disabled></td>
                                </tr>
                                <tr>
                                    <td class="label-data"><label>Email Address</label></td>
                                    <td><input type="email" value="<%= userVer.email %>" disabled></td>
                                </tr>
                            </table>
                        </div>                        
                        <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#profileModal">Edit Profile</button>
                        <% if(userVer.password) { %>
                            <a href="/changepassword"><button class="btn btn-primary" data-bs-toggle="modal">Change Password</button></a>
                        <% } %>
                    </div>
                </div>
                <form id="logoutForm" action="/logout" method="post" style="display: none;"></form>

                <!-- Profile Modal -->
                <div class="modal fade" id="profileModal" tabindex="-1" aria-labelledby="profileModalLabel"
                    aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="profileModalLabel">Edit Profile</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"
                                    aria-label="Close"></button>
                            </div>
                            <div class="modal-body">
                                <form id="user-details">
                                    <div class="mb-3">
                                        <label for="modalFullName" class="form-label">Full Name</label>
                                        <input type="text" class="form-control" id="modalFullName" value="<%= userVer.name %>">
                                        <p class="name-error" style="color: red;"></p>
                                    </div>
                                    <div class="mb-3">
                                        <label for="modalPhoneNumber" class="form-label">Phone Number</label>
                                        <input type="text" class="form-control" id="modalPhoneNumber"
                                            value="<%= userVer.phone %>">
                                        <p class="phonenumber-error" style="color: red;"></p>
                                    </div>
                                    <button type="submit" class="btn btn-success">Save Changes</button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Footer -->
                <div class="footer mt-4">
                    <div class="row">
                        <div class="col-md-3">
                            <h6>Contact Us</h6>
                            <p>Address: Leeds, London, UK - 1290</p>
                            <p>Phone: 002 52695 24542</p>
                            <p>Fax: 002 78985 369552</p>
                            <p>Email: support@example.com</p>
                        </div>
                        <div class="col-md-3">
                            <h6>Information</h6>
                            <ul>
                                <li><a href="">Your Account</a></li>
                                <li><a href="">Free Shipping Policy</a></li>
                                <li><a href="">Your Cart</a></li>
                                <li><a href="">Return Policy</a></li>
                                <li><a href="">Delivery Info</a></li>
                            </ul>
                        </div>
                        <div class="col-md-3">
                            <h6>Support</h6>
                            <ul>
                                <li><a href="#">Payment Method</a></li>
                                <li><a href="#">Help</a></li>
                                <li><a href="#">Privacy Policy</a></li>
                            </ul>
                        </div>
                        <div class="col-md-3">
                            <h6>Join Our Mailing List</h6>
                            <form>
                                <input type="email" class="form-control" placeholder="Your Email Address">
                                <button class="btn btn-dark mt-2">Subscribe</button>
                            </form>
                            <p>Download Our Mobile Apps:</p>
                            <img src="/images/playstore.webp" alt="Google Play" style="max-width: 100px;">
                            <img src="/images/appstore.png" alt="App Store" style="max-width: 100px;">
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Select the input fields and error messages
        const nameInput = document.getElementById('modalFullName');
        const phoneInput = document.getElementById('modalPhoneNumber');
        const nameError = document.querySelector('.name-error');
        const phoneError = document.querySelector('.phonenumber-error');

        // Validate Name on Blur
        nameInput.addEventListener('blur', () => {
            const nameValue = nameInput.value.trim();
            const nameRegex = /^[a-zA-Z\s]+$/; // Allows alphabets and spaces
            if (!nameValue) {
                nameError.textContent = 'Name is required.';
            } else if (!nameRegex.test(nameValue)) {
                nameError.textContent = 'Name must contain only alphabets.';
            } else {
                nameError.textContent = ''; // Clear error
            }
        });

        // Validate Phone Number on Blur
        phoneInput.addEventListener('blur', () => {
            const phoneValue = phoneInput.value.trim();
            const phoneRegex = /^[0-9]{10}$/; // Allows exactly 10 digits
            if (!phoneValue) {
                phoneError.textContent = 'Phone number is required.';
            } else if (!phoneRegex.test(phoneValue)) {
                phoneError.textContent = 'Phone number must be exactly 10 digits.';
            } else {
                phoneError.textContent = ''; // Clear error
            }
        });

        // Prevent form submission if there are validation errors
        document.getElementById('user-details').addEventListener('submit', async (event) => {
            if (nameError.textContent || phoneError.textContent) {
                event.preventDefault();
            } else {
                event.preventDefault();
                const newData = {
                    name: $('#modalFullName').val(),
                    phone: $('#modalPhoneNumber').val(),
                };
                try {
                    let response = await fetch('/edituserdata', {
                        method: 'PATCH',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(newData)
                    });
                    const result = await response.json();
                    const toastContainer = document.querySelector(".toast-container");
                    const toast = document.createElement("div");
                    toast.className = "toast";
                    if (result.success) {
                        toast.textContent = result.message;
                        toast.style.backgroundColor = "#28a745"; // Green color
                        toastContainer.appendChild(toast);
                        setTimeout(() => {
                            toast.style.display = "block";
                        }, 100);

                        setTimeout(() => {
                            toast.style.animation = "slideOutToRight 0.8s ease-out forwards";
                        }, 2000);

                        setTimeout(() => {
                            toast.style.display = "none";
                            toast.remove(); // Remove toast from DOM
                            window.location.href = "/profile"
                        }, 2800);
                    } else {
                        toast.textContent = result.message;
                        toast.style.backgroundColor = "#dc3545"; // Red color for error
                        toastContainer.appendChild(toast);
                        setTimeout(() => {
                            toast.style.display = "block";
                        }, 100);

                        setTimeout(() => {
                            toast.style.animation = "slideOutToRight 0.8s ease-out forwards";
                        }, 2000);

                        setTimeout(() => {
                            toast.style.display = "none";
                            toast.remove(); // Remove toast from DOM
                        }, 2800);
                    }
                } catch (error) {
                    console.log(error)
                    return;
                }
            }
        });
    </script>
</body>

</html>