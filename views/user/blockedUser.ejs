<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Oops, You're Blocked!</title>
  <link rel="icon" href="/images/logo.png">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Source+Sans+3:ital,wght@0,200..900;1,200..900&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Source Sans 3', sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #e1e3df,#8CBF3A);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      color: #333;
    }

    .blocked-container {
      text-align: center;
      padding: 30px;
      border: #528008;
      border-radius: 20px;
      background-color: #fff;
      box-shadow: 10px 10px 15px rgba(0, 0, 0, 0.5);
      animation: bounce 1s infinite alternate;
    }

    @keyframes bounce {
      0% { transform: translateY(0); }
      100% { transform: translateY(-10px); }
    }

    .blocked-icon {
      font-size: 60px;
      color: #e63946;
      margin-bottom: 15px;
      animation: spin 15s linear infinite;
    }

    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    .blocked-message {
      font-size: 20px;
      font-weight: bold;
      color: #e63946;
      margin-bottom: 10px;
    }

    .help-text {
      font-size: 14px;
      color: #555;
      margin-bottom: 20px;
    }

    .button-container {
      display: flex;
      gap: 20px;
      justify-content: center;
    }

    .contact-button, .logout-button {
      display: inline-block;
      padding: 12px 25px;
      font-size: 16px;
      font-weight: bold;
      color: white;
      text-shadow: 2px 2px #333;
      border: none;
      border-radius: 10px;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.5s ease;
    }

    .contact-button {
      background-color: #8CBF3A;
    }

    .contact-button:hover {
      background-color: #45A049;
      transform: scale(1.2) rotate(-5deg);
    }

    .logout-button {
      background-color: #ff6b6b;
    }

    .logout-button:hover {
      background-color: #e63946;
      transform: scale(1.2) rotate(5deg);
    }

    .footer-note {
      margin-top: 20px;
      font-size: 12px;
      color: #777;
      font-style: italic;
    }
  </style>
</head>
<body>
  <div class="blocked-container">
    <div class="blocked-icon">🚫</div>
    <div class="blocked-message">Oops! You're Blocked! 🛑</div>
    <div class="help-text">We're sorry, but this is where the fun ends. Contact support to fix this!</div>
    <div class="button-container">
      <a href="https://mail.google.com/mail/?view=cm&fs=1&to=imbedsoftwareteam@gmail.com" class="contact-button">Contact Support 📧</a>
      <form action="/logout" method="post"><button type="submit" class="logout-button">Logout 🚪</a></form>
    </div>
    <div class="footer-note">P.S. Don't worry, we still like you... sort of. 😉</div>
  </div>
</body>
</html>
